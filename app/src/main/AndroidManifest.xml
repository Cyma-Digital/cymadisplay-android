<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- WiFi provisioning: detect connectivity, scan/join networks, run the
         setup hotspot. ACCESS_FINE_LOCATION and NEARBY_WIFI_DEVICES back the
         scan + LocalOnlyHotspot APIs; the app self-grants them at runtime via
         its device-owner privilege (see admin.DeviceOwnerManager). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- WRITE_SETTINGS backs the AP-config write on BOTH the legacy tethering-hotspot
         path (API < 26) AND the fixed-credential tethering tier tried first on API
         26+ (see SoftApController.startTetheredHotspot) before it falls back to
         OS-random LocalOnlyHotspot. On API 23+ it is gated at runtime by an appop;
         the warehouse provisioning step runs
         `adb shell appops set com.cyma.videoloop WRITE_SETTINGS allow` on EVERY box
         (not just pre-O ones) to enable it. Auto-granted on install below API 23. -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:minSdkVersion="33" />
    <!-- Enabling "Draw over other apps" sets the SYSTEM_ALERT_WINDOW appop to
         MODE_ALLOWED, which grants the background-activity-start exemption the
         boot receiver needs to relaunch the app after a reboot on ROMs that
         block background starts (e.g. Amlogic TX3 mini+ Android 13). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Cyma Display"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoLoop">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:exported="true"
            android:theme="@style/Theme.VideoLoop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".kiosk.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Device-admin receiver. Provisioned once per box at the warehouse via
             `adb shell dpm set-device-owner com.cyma.videoloop/.admin.CymaAdminReceiver`
             followed by `adb shell appops set com.cyma.videoloop WRITE_SETTINGS allow`
             on every box, regardless of API level (backs both the legacy tethering
             path on API < 26 and the fixed-credential tethering tier on API 26+).
             Device-owner status is what lets the app silently self-grant the
             location permission and program the WiFi config during setup. -->
        <receiver
            android:name=".admin.CymaAdminReceiver"
            android:description="@string/device_admin_description"
            android:label="@string/device_admin_label"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
